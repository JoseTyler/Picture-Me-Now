{{Cat.name}}

<div class="category">
{{#each photographers.photos}}
<a class="noUnderline" href="/{{_id}}">{{name}}</a>
name: {{name}} 
{{/each}}
</div>
<form action="/{{Cat._id}}?_method=DELETE" method="POST">
    <button type="submit">Delete Category</button>
</form>